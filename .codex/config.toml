[project]
name = "quant-intel-brief"
description = "Daily quantitative-finance intelligence brief scaffold."

[agent]
primary_instructions = "Follow AGENTS.md and PLANS.md. Preserve the local-first, source-grounded implementation boundaries."

[checks]
lint = "ruff check ."
tests = "pytest"
